# avsar-live (staged — not deployed)

V2 worker: overlays exact deadlines / open-now status / apply links onto the static site. Full design in [`../PLAN-V2.md`](../PLAN-V2.md), activation checklist at the bottom of that file. Deployable as-is (returns an empty overlay) on the vaibhavpro9210 Cloudflare account once `AVSAR_KV` is created; source watchers and LLM keys unlock slices independently. No secrets live in this repo.
